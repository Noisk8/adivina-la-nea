let num = "10";

// cuando solo tiene un parametro se puede quitar el parentesis  y se puede poner en una sola linea

// si recibe varios parametros  let sumarDecena = (param1, param2) => numero + 12

let sumarDecena = (numero) => numero + 12;

const respuesta = sumarDecena(parseInt(num));

console.log(respuesta);
