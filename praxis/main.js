// Add JS her
const variable = "mera vuelta";

console.log(variable + 1);

/*


const | se puede declarra sin valor, no se puede reasignar, no se puede redeclarar 

var | se puede declarar sin valor, se puede reasignar, se puede sedeclarar

let | se puede declarar sin valor, se puede se reasignar, no se puede redeclarar 
*/
